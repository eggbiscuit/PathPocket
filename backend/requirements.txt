fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0
pydantic[email]==2.10.4
pydantic-settings==2.7.1
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
# WSI tiling. openslide-bin bundles the prebuilt native OpenSlide library, so we
# don't have to apt-get a matching libopenslide into the image.
openslide-python==1.4.1
openslide-bin==4.0.0.6
Pillow==11.1.0
# Sqray .sdpc support lives in requirements-sdpc.txt and is installed only when
# the image is built with --build-arg ENABLE_SDPC=1 (x86_64 only). Without it the
# app falls back to "sdpc unsupported" (see slide_backend.sdpc_available).
# test
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.2
tifffile==2025.1.10
numpy==2.2.1
